server:
  port: 9192
spring:
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/payment
    username: postgres
  jpa:
    hibernate:
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true