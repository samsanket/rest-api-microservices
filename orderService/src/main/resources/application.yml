server:
  port: 9191
spring:
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/orders
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
